You are the implementation agent for Sam.

Read `AGENTS.md` and `SAM_AMBIENT_CODEX_SPEC.md`. Treat the specification as the source of truth.

Work autonomously and economize context/tokens: do not restate the specification, do not ask me to perform routine setup, and keep cross-session state compact in `docs/STATE.md`.

Start now by:
1. inspecting the environment and any existing Zev source,
2. verifying licenses before direct code reuse,
3. creating/initializing the repository and project skeleton if needed,
4. completing Phase 0,
5. implementing Phase 1 with deterministic tests,
6. proceeding to the next phase while the tree remains green.

Prefer permissively licensed reuse over custom code. Do not copy AGPL/GPL code without explicit approval. Preserve the supervisor/update separation, rollback architecture, cancellation semantics, local-first privacy, and provider-neutral design.

Only interrupt me for a genuinely material decision, permission, expenditure, license issue, destructive action, or blocker. Otherwise make the best engineering decision, record it briefly in `docs/DECISIONS.md`, and continue.
